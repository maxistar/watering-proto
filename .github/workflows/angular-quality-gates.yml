name: Angular Quality Gates

on:
  pull_request:
    paths:
      - 'watering-proto/**'
      - '.github/workflows/angular-quality-gates.yml'
  workflow_dispatch:

jobs:
  lint-and-unit:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: watering-proto
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: watering-proto/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint

      - name: Run unit tests
        run: npm run test:ci
